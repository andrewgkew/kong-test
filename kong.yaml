_format_version: "3.0"
plugins:
  - name: prometheus
    config:
      per_consumer: true
services:
  - name: echo-service
    path: /anything
    host: httpbin.org
    port: 1027
    protocol: https
    routes:
      - name: echo-route
        methods:
          - GET
        paths:
          - /echo
