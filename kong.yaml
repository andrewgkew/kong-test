_format_version: "3.0"
plugins:
  - name: prometheus
    config:
services:
  - name: echo-service
    path: /anything
    host: httpbin.org
    port: 443
    protocol: https
    routes:
      - name: echo-route
        methods:
          - GET
        paths:
          - /echo
